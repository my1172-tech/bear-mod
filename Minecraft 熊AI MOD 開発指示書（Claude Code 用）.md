# Minecraft 熊AI MOD 開発指示書（Claude Code 用）

## 目的
Minecraftにおいて、**市街地へ侵入し生活圏の資源（食料）を探索・略奪する熊AI**を実装する。  
単純な敵モブではなく、**徘徊ルート・個体差・家屋侵入・チェスト略奪・確率破壊・強力攻撃・遠距離ダメージでの逃走**を統合した野生動物シミュレーションとして設計する。

---

## 最重要ルール（Claude Code）
1. 既存リポジトリを必ず調査する（Mod Loader / Minecraftバージョン特定）
2. 既存構造を破壊しない
3. 推測でAPIを使わない（実コード確認）
4. 段階実装（Phase方式）
5. 設定値は外出し
6. ビルド・検証を毎段階で実施

---

## 機能要件一覧
- 市街地周辺徘徊
- 複数ルートパターン
- 個体ごとの性格
- 家屋検知・侵入
- チェスト探索
- 食料のみ略奪
- 確率でチェスト破壊
- プレイヤー攻撃（1～3撃）
- 遠距離攻撃で逃走

---

## 状態マシン設計

```
IDLE
↓
PATROL（徘徊）
↓
SEARCH_HOUSE（家探索）
↓
ENTER_HOUSE（侵入）
↓
SEARCH_CHEST
↓
LOOT（略奪）
↓
ATTACK（攻撃）
↓
ESCAPE（逃走）
↓
RETURN（復帰）
```

---

## AI優先順位

1. ESCAPE
2. ATTACK
3. LOOT
4. ENTER_HOUSE
5. SEARCH_HOUSE
6. PATROL

---

## 個体特性（ランダム）

| パラメータ | 内容 |
|---|---|
| aggression | 攻撃性 |
| bravery | 度胸 |
| hunger | 空腹 |
| foodSeeking | 食料優先 |
| urbanPreference | 市街地志向 |

```
0.0 ～ 1.0
```

---

## 行動ロジック

### 攻撃
- 1～3撃でプレイヤー死亡
- 個体差あり

### 逃走
条件：
- 遠距離攻撃
- 一定ダメージ以上

```
if projectileDamage > threshold → ESCAPE
```

---

## チェスト処理

### 探索
```
半径16～32
対象：chest
```

### 食料判定
- FoodComponent使用

### 略奪
- 食料のみ取得

### 破壊
```
確率：30%
```

---

## 家屋侵入

### 判定
- ドアを基準

### 行動
```
ドア発見
↓
接近
↓
開く or 破壊
↓
侵入
```

---

## 徘徊ルート

### Route A（水路型）
森林 → 川 → 農地 → 家

### Route B（道路型）
森林 → 道路 → 家

### Route C（境界型）
山 → 市街地端 → 戻る

### Route D（食料型）
家 → チェスト → 次の家

### Route E（侵入型）
住宅地巡回

---

## ルート選択

```
spawn時にランダム
＋特性で重み付け
```

---

## スポーン

- 森林
- 山
- 市街地外周

---

## パフォーマンス

禁止：
- 毎tick全探索

推奨：
```
探索間隔：10～20tick
```

---

## 設定値（外出し）

```
BEAR_HEALTH
BEAR_DAMAGE
CHEST_RANGE
HOUSE_RANGE
ESCAPE_THRESHOLD
ESCAPE_TIME
BREAK_CHANCE
```

---

## テストチェック

- [ ] 熊スポーン
- [ ] 徘徊動作
- [ ] 家侵入
- [ ] チェスト略奪
- [ ] 食料のみ取得
- [ ] チェスト破壊確率動作
- [ ] 攻撃1～3撃
- [ ] 遠距離逃走
- [ ] パフォーマンス正常

---

## 実装順序（必須）

1. Entity作成
2. 徘徊AI
3. 個体特性
4. 攻撃
5. 逃走
6. 家侵入
7. チェスト探索
8. 略奪
9. 破壊
10. ルート統合
11. 最適化

---

## 完成条件

- 自然な徘徊
- 家侵入が発生
- 食料略奪が機能
- プレイヤーに脅威
- 逃走が成立
- 個体差が体感できる

---

## 設計思想

> 「敵モブ」ではなく  
> **野生動物シミュレーションとして設計する**

---

## 最終イメージ

```
森林スポーン
↓
ルート徘徊
↓
家発見
↓
侵入
↓
チェスト略奪
↓
破壊（確率）
↓
プレイヤー遭遇
↓
攻撃
↓
遠距離攻撃
↓
逃走
↓
再徘徊
```

---

## 実機所見（2026-08-14 追記）

初回実機の症状は **「コマンドは発動するが熊がいない」**。
入れ先は**教育版**、ワールド `PLATEAU_20260810`（BP・RPとも有効）。

ワールドの `db` を生バイトで直読みして実測した（推測しない）:

| 見たもの | 実測 | 意味 |
|---|---|---|
| `bear:bear` | 440件・205地点 | 熊は湧いて動いていた（コマンドも定義も正常） |
| `bear:mode_roam` | 220件 | 状態機械も走っていた |
| `bear:mode_hunt` | 12件 | プレイヤーの24m以内に来た瞬間もある |
| **`bear:lure`** | **0件** | **目印が一度も生まれていない** |

→ 「熊がいない」のではなく、**行き先を持てず、バニラのうろつきだけをしていた。**

### 真因

1. 打ち切った走査が `onDone` を呼ばずに捨てられ、頼んだ熊の `pending` が**永久に true**
2. ブロックの `typeId` / `location` は**読むだけで例外が飛ぶ**（読み込み範囲から外れた瞬間）のに素で触っていた
3. 毎tickの入口（走査の進行）が素だったので、1か所の例外で**MOD全体が黙って止まる**

静的検査・机上試験は7本すべて通っていた。**通ることは動くことではない。**

### 以後の決まり（この指示書の一部として守る）

1. **非同期の返事は必ず返す。** 成功・打ち切りの両方で呼ぶ。待ち手を放置しない
2. **時間で必ず解く安全弁を置く。** 返事が来ない理由は呼び手から分からない（`PENDING_TIMEOUT`）
3. **ブロックと実体は「読み取りだけでも例外が飛ぶ」前提で包む。** 参照が取れたことは読めることを意味しない
4. **毎tickの入口は必ず包む。** 1か所の失敗で全体を殺さない
5. **失敗を黙って諦めない。** 理由をコマンドの返事に出す（`bear_spawn` は例外文、`bear_status` は目印の状態と各熊の行き先）
6. 直したら**逆テストを足す**。「起きてはいけないことが起きない」側を機械で見張る

### 実機チェックの追加項目

- [ ] `bear_status` の各行に行き先（`→座標`）が出る＝目印が生まれている
- [ ] `bear_spawn` の返事に座標が出て、その場所に実際にいる
- [ ] `bear_debug_on` の名札が出るのに体が見えない → リソースパックが効いていない

---

## 実機所見 その2（2026-08-15 追記）

症状は **「熊が詰まって動けない、といっぱい出る。地面の中に埋まっているように見える」**。

### 真因

**当たり判定の幅を 1.3 で作っていた。**

統合版は、当たり判定がブロックに食い込んだ実体を**押し出してくれない**。
幅 1.3 の熊は**幅1のドアも路地も通れない**ので、家に入ろうとして入口に
食い込んだまま一歩も動けなくなる。外から見ると壁や地面に埋まっている。

指示書の「家屋侵入」はドアを通ることが前提なので、幅 1.3 では
**要件そのものが物理的に成立していなかった**。模型の胴は 12/16 = 0.75
ブロック幅しかないので、当たり判定だけが太っていた。

### 直したこと

| | 直した中身 |
|---|---|
| 1 | 当たり判定を 0.9 に（`BEAR_WIDTH`。**1.0未満**を `check_config.py` が見張る） |
| 2 | 詰まったら**本当に食い込んでいるか**を確かめ、食い込んでいれば近くの立てる場所へ掘り出す |
| 3 | 湧かせる場所は**幅ぶんの足場**と**そこから出られる道**があること（囲まれた1マスの穴に湧かせない） |
| 4 | 途中の地面の高さが測れないときは**目印を置かない**（地中の目印には熊が永久に届かない） |
| 5 | 略奪中・ドアを叩いている間・張り付いている間は**詰まりに数えない** |
| 6 | 同じ理由のログは間隔を空ける／`bear_status` に 詰・埋・掘り出し回数を出す |

### 以後の決まり（追加）

7. **物理的に成立するかを先に確かめる。** 「通れるはず」で作らない。
   ドアは幅1・熊は幅1.3、という**数字の突き合わせ**は机上でできたはずだった
8. **「動かない」を一括で救済しない。** 止まっているのが正しい場面と、
   本当に動けない場面を分ける。分けずに動かすと徘徊が瞬間移動になる
9. **見えない当たり判定は、見える模型と別物**。見た目が細くても当たり判定は太い

### 実機チェックの追加項目

- [ ] 熊がドアをくぐって家の中まで入る（入口で止まらない）
- [ ] `bear_status` の `足が止まっている熊` が頭数ぶんに張り付かない
- [ ] `[埋]` が出ない（出るなら、そこは掘り出し先の無い場所）

---

## 実機所見 その3（2026-08-15 追記・コンテンツログ実測）

教育版 `PLATEAU_20260810` で v0.2.0 を動かし、コンテンツログを実測した。
`[bear]` の行は **489 行。そのうち 202 行（41%）が「詰まったので道のりを引き直す」**。

### 真因1：区間を繋ぐ呼び出しが無かった（詰まり41%）

目印は `WAYPOINT_MAX` より遠くへ置けない。遠い行き先はその距離ずつ繋ぐ必要が
あり、そのための `reachedWaypoint()` も**書いてあった。しかし
どこからも呼んでいなかった。**

熊は途中の目印に着いた時点で行き先を失い、その場で止まり、3秒後に
「詰まった」と判定されて道のりごと捨てる。これを延々と繰り返していた。

→ `advanceWaypoint()` を足し、`update()` から毎周期呼ぶ。

### 真因2：目印がワールドの動く範囲の外だった

`目印を出せない: LocationInUnloadedChunkError` が実測で出ていた。
`WAYPOINT_MAX` が 56m で、統合版の既定シミュレーション距離（4チャンク＝64m）を
はみ出していた。範囲外の目印は動かないので熊の索敵に映らない。

→ `WAYPOINT_MAX = 32`。`check_config.py` が 32 超を弾く。

### 真因3：アニメが一度も動いていなかった（脚が止まったまま滑る）

`[Animation][error] entity/bear.entity.json | description | animation_controllers |
child 'animation_controllers' not valid here.`

`minecraft:client_entity` の `description` に `animation_controllers` は書けない。
`animations` に短い名前で登録し、`scripts.animate` で回す。**しかも検査(validate.py)
自体がこの誤った書き方を正しいものとして検証していた。**

### その他

- `scan_interval: 4` → 統合版が毎回 `[Json][error]` を吐く。10 に上げた
- PLATEAU都市ワールドには **ドアが1枚も無い**（engine_plateau は door を置かない）。
  そのため `SEARCH_HOUSE → PATROL (家が無い)` が19回。**家侵入・チェスト略奪は
  この種のワールドでは成立しない**（要判断・未着手）

### 以後の決まり（追加）

10. **書いたら繋ぐ。** 未使用の export は無言の不具合の温床。`validate.py` が
    「どこからも呼んでいない export」を弾く（`reachedWaypoint` はこれで見つかった）
11. **検査が正しいとは限らない。** 真因3は、検査が誤った書き方を追認していた。
    実機のログに出たエラーは、検査を疑う合図として扱う
12. **足した検査は、わざと壊して落ちることを確かめる。** 落ちない検査は飾り

### 実機チェックの追加項目

- [ ] `[bear]` のログに「詰まった」が並ばない（数字付きで出る: `目印まで○m 行き先まで○m 目印○`）
- [ ] 歩いている熊の**脚が動く**（滑らない）
- [ ] コンテンツログに `[Json][error]` `[Animation][error]` が出ない

---

## 実機所見 その4（2026-08-15 追記・v0.4.0〜v0.8.0）

一度に5件の症状を潰した。**すべて実機からの報告が起点**で、机上試験は毎回
全部通っていた。

### 1. 窓からの侵入（PLATEAUにドアが1枚も無い）

`engine_plateau` は door を置かない。ドアだけを入口にすると、PLATEAUの都市
ワールドでは**家侵入もチェスト略奪も原理的に成立しない**。窓（ガラス）を
入口に加えた。

生成側の作り（`converter/facade.py`）を読んで合わせてある。

- 一軒家の窓は**地面から3段目のガラス1段**。約45%だけ2段（大窓）
- ガラスビルは腰壁2段の上から全面ガラス
- 窓ブロックは 青45 / 水色25 / 透明20 / 遮光10（ビル）、薄灰・水色・灰・白・茶（一軒家）

ガラス1段では穴が1段しか開かず、高さ1.4の熊は通れない。そこで
**ガラスとその上下1段（窓枠）** をぶち抜く。守る線は3つ。

| | |
|---|---|
| `WINDOW_STEP_UP = 1` | 開口の下端は足元から1段以内。超えると割ったのに入れない |
| 外の地面より下は掘らない | 掘ると窓ではなく穴になる |
| `WINDOW_OPEN_MAX = 3` | ガラス張りのビルで壁面が丸ごと消えないように |

割るかどうかは確率（`WINDOW_BREAK_CHANCE`）。**空腹と攻撃性で上がる**
（実測 満腹53% / 腹ぺこ97%）。0 にすると1枚も割らない。

### 2. 「窓を割っても中に入らない」の真因＝床の測り方

`windowInside` が `groundY` を使っていた。**`groundY` は「いちばん上の
ブロック」から下へ探すので、家の中では屋根の上を返す。** それを床と信じると

```
屋根の高さ − 屋根の上の立ち位置 = 負 → 「屋根が無い＝中ではない」
```

となり、必ず外れる。窓の高さから下へたどる `floorNear()` に置き換えた。
**ドアが無事だったのは、ドア側がドア自身の高さ（＝床の高さ）を使っていたため。**

### 3. 視界と嗅覚を分ける

| | 距離 | 視野角 | 壁 |
|---|---|---|---|
| 通常 | 40m | 180° | 通らない |
| 警戒 | 64m | 140° | 〃 |
| 発見 | 90m | 120° | 〃 |
| 追跡 | 120m | 120° | 〃 |

嗅覚は**視野角が無く、壁を通り抜ける**。料理150 / 落ちている肉200 / 牛200 /
羊豚130 / 動物110 / 人100。強さは 焼いた肉100 > 生肉80 > 魚70 > 木の実40 > 人10。
選び方は**強さ ÷ 距離**。

料理中は統合版のAPIに無いので、**火の入ったかまど・燻製器・焚き火**で見分ける。

ここで2つ踏んだ。

- `acos` の丸めで、視野角180°の熊が**真横（ちょうど90°）を見落として**いた
- **見つけた瞬間に視野が180→120°へ狭まり、直前に見た相手を見失う**ループ。
  1周期に1回だけ見る＋`SIGHT_NEAR = 12` 以内は向きを問わない、で解決

### 4. 「襲われても速くならない」

突進が **1.3倍（0.325）**、徘徊が 1.0倍（0.25）で差が3割しかなく、
しかも逃走 1.7倍より遅かった。徘徊1.0 / 突進1.8 / 逃走1.9 に改め、
3つとも config へ出して `check_config.py` が
「突進は徘徊の1.4倍以上」「逃走は突進より速い」まで見張る。

### 5. 「目印を出せない」は不具合ではなかった

`LocationInUnloadedChunkError` は「その熊は**ワールドが動かしていない範囲**に
いる」だけ。熊自身もそこでは動いていないので目印は要らず、近づけば直る。
数えて穏当に出すだけにした。加えて**成功しても古い理由を消さない経路**があり、
一度失敗すると赤字が出続けていた（本物の失敗が埋もれる）。

### 6. 空腹が増える仕組みが無かった

`hunger` は食べると下がる仕組みだけがあり、**上がる仕組みが無かった**。
一度食べた熊は二度と腹が減らない。`HUNGER_GAIN` で時間とともに増える
（満腹→空腹で約6分）。

---

## 以後の決まり（追加）

13. **たまに落ちる試験は不具合と同じ。** 本物の失敗と見分けが付かない。
    今回3つ潰した。
    - 確率を見る試験は**幅を6σ取る**（2000回×±0.03 は2.9σで、数十回に1回外れる）
    - 距離が絡む試験は**距離の揺れを消す**（熊は湧いた直後から歩き出す）
    - **本体(main.js)と取り合わない**。速さを測る試験で main.js を一緒に回すと、
      向こうがモードを決め直して別のものを測ることになる
14. **試験に数字を写さない。** 机上の物理が速さを写し持っていたため、
    調整値を変えても古い速さで通っていた。`config.js` から読む
15. **「たまたま入れた」で満足しない。** 窓の試験は1軒だけだと通ってしまう。
    **2軒つづけて**入れるかで見る（1軒目だけ入れる壊れ方が実在した）
16. **同じ数字が2か所にあるものは、必ず検査で突き合わせる。**
    体力・移動速度・当たり判定・**速さの倍率**・版番号がこれにあたる

---